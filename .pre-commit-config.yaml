# https://pre-commit.com — install: `poetry run pre-commit install`
repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check (full tree)
        entry: poetry run ruff check .
        language: system
        pass_filenames: false
        always_run: true
