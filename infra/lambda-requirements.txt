# Runtime dependencies for Lambda ZIP deploy (keep in sync with [tool.poetry.dependencies] in pyproject.toml).
fastmcp==3.2.0
fastapi>=0.111
httpx>=0.27
pydantic-settings>=2.0
pydantic>=2.0
python-jose[cryptography]>=3.3
mangum>=0.17
tenacity>=8.2
